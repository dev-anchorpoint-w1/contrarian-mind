# The Contrarian Mind

A thinking tool. One consciousness forged from 2,400 years of dissent.

## Setup

1. Clone the repo
2. Copy `.env.example` to `.env` and add your Anthropic API key
3. `npm install`
4. `npm run dev`

## Deploy

Push to Vercel. Set `ANTHROPIC_API_KEY` in the Vercel dashboard.
